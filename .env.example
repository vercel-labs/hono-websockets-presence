# Redis from the Vercel Marketplace (https://vercel.com/marketplace/redis).
# REDIS_URL (a rediss:// connection string) is injected automatically when you
# link the integration; set it locally to share presence across instances.
# Without it, the server falls back to an in-memory store (single-instance only).
REDIS_URL=
